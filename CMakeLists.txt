cmake_minimum_required(VERSION 3.12)
project(libserialport C)

# --- 1. Handle config.h for Autotools Compatibility ---
if(NOT WIN32)
    if(NOT EXISTS "${CMAKE_CURRENT_BINARY_DIR}/config.h")
        file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/config.h" "/* Generated by CMake for compatibility */\n")
    endif()
    include_directories(${CMAKE_CURRENT_BINARY_DIR})
endif()

# --- 2. Source Files Configuration ---
set(SERIALPORT_SOURCES
    serialport.c
    timing.c
)

if(WIN32)
    list(APPEND SERIALPORT_SOURCES windows.c)
    set(WINDOWS_LIBS setupapi cfgmgr32)
elseif(APPLE)
    list(APPEND SERIALPORT_SOURCES darwin.c)
    find_library(FRAMEWORK_IOKIT IOKit)
    find_library(FRAMEWORK_COREFOUNDATION CoreFoundation)
    set(OS_LIBS ${FRAMEWORK_IOKIT} ${FRAMEWORK_COREFOUNDATION})
else()
    list(APPEND SERIALPORT_SOURCES linux.c)
endif()

# --- 3. Core Library Target ---
add_library(serialport ${SERIALPORT_SOURCES})

# Using SYSTEM forcing tells GCC/Clang to inspect this directory for angle-bracket <headers>
target_include_directories(serialport SYSTEM PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)

if(WIN32)
    target_link_libraries(serialport PRIVATE ${WINDOWS_LIBS})
    target_compile_definitions(serialport PRIVATE SP_PRIV=)
elseif(APPLE)
    target_link_libraries(serialport PRIVATE ${OS_LIBS})
endif()

# --- 4. Examples Build Configuration ---
if(IS_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/examples")
    add_executable(list_ports examples/list_ports.c)
    target_link_libraries(list_ports PRIVATE serialport)
    
    # Explicitly bound target include path to make certain <libserialport.h> resolves instantly
    target_include_directories(list_ports PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/include)
endif()
